import { updateUI } from '../src/client/js/showResults'

describe('Testing the Show results functionality', () => {
    test('Check if updateUI is defined correctly', () => {
        expect(updateUI).toBeDefined();
    })
})